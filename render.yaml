services:
  - type: web
    name: yt-downloader
    env: python
    buildCommand: apt-get update -y && apt-get install -y ffmpeg nodejs && pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 2
    plan: free